package com.google.interview.questions.searching;

/**
 * Same as quick sort but instead of
 * recursing into both sides quickselect
 * only recurses into one side – the
 * side with the element it is searching
 * for. This reduces the average
 * complexity from O(n log n) to O(n),
 * with a worst case of O(n2).
 * 
 * @author VijaySidhu
 *
 */
public class QuickSelect {

}
